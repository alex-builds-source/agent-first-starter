# agent-first-starter v0.1.3 shipped

## What shipped
- Added starter presets: `cli-only`, `api-only`, and `cli-api`.
- Added optional scaffolds: `--with-precommit` and `--with-release-workflow`.
- Expanded generated outputs (`pyproject.toml`, preset-aligned docs/src) and tightened tests.

## Why it matters
- You can start with a minimal shape and avoid deleting files after scaffolding.
- Optional automation pieces stay opt-in, so generated projects remain lean.

## Validation
- Tests: 4 passed
- Secret scan: clean

## Links
- Repo: https://github.com/alex-builds-source/agent-first-starter
- Release: https://github.com/alex-builds-source/agent-first-starter/releases/tag/v0.1.3
